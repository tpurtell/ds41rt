# DS41RT v10 compact EXL3 TP3 1x / 3-Spark: performance and qualification

Generated by `scripts/render-ds41-v10-tp3-reports.py` from the raw bench outputs named below. Every cell is derived from a recorded sample; an em dash (—) or a **PENDING** cell means no qualifying measurement is present in this package, not that a run was never attempted. Nothing is estimated, interpolated or substituted from another campaign.

> **Status: NOT EXECUTED - pending raw manifests; no performance, memory, quality or readiness claim**

- Generated (UTC): 2026-09-21T13:36:40Z
- Release: `v10`
- Checkpoint: `deepseek-ai/DeepSeek-V4.1-Flash` @ `dba1be0a40aa45a94ad051997016db3960a90277` (`official`)
- Images: PENDING — the v10 pair is not published yet, so no image identity is claimed.
- Network: PENDING — no negotiated link evidence captured.
- Change thresholds: PENDING — no per-campaign spread basis recorded.

## Campaign status

| Arm | Quant | Topology | RTX | Sparks | Expected records | Actual records | Status |
|---|---|---|---:|---:|---:|---:|---|
| v10-exl3-compact-tp3 | exl3 | TP3 | 1 | 3 | 359 | 0 | NOT EXECUTED (pending raw manifests) |

## Configurations

- **v10-exl3-compact-tp3** — exl3 `TP3` (implicit), 1 RTX + 3 Spark. config sha256 `087e3498307bffbe97dcb0e333240884f5fddbe3563adb0d492e6d54908984cf` (`examples/configs/exl3-compact-tp3.config`).
  - resolved: dspark=on, exl3_family=k34, expert_format=exl3, kv_pool_size=2GiB, memory_reservation=32GiB, prefill_batch_tokens=256, spark_count=3, spark_ep=1, spark_tp=implicit (count = TP)
  - warmup: one shape warmup, excluded from the ledger; repeats: 3
  - comparison group: `v10-1rtx-3spark`

## Weight, residency and memory ceiling

Whether an arm is capped is part of its identity: the compact EXL3 arm is measured under a 32 GiB reservation, the native arm is uncapped, so their throughput numbers are not interchangeable.

| Arm | Topology | RTX | Sparks | Resident bytes/rank | Remote layers | Worker allocated layers | Memory reservation | KV pool | Prefill batch tokens |
|---|---|---:|---:|---:|---:|---:|---|---|---:|
| v10-exl3-compact-tp3 | TP3 | 1 | 3 | — | — | — | 32GiB | 2GiB | 256 |

- **v10-exl3-compact-tp3**: Compact EXL3 TP3 is the implicit layout (SPARK_COUNT=3 with no SPARK_TP/SPARK_EP); each rank serves the 2304/3 = 768-column shard from non-paired disjoint k34-family packages. Resident bytes are not yet captured and are not inferred from the native arm.
- **v10-exl3-compact-tp3** ceiling note: Declared ceiling from examples/configs/exl3-compact-tp3.config. The resolved reservation, occupancy and KV figures must be captured from the service at launch; the declared values are not a proven fit.

## Expected performance-record accounting

The expected counts below are **derived**, not asserted: each family's expectation is recomputed from the live harness scripts' own constants and argparse defaults (`DEFAULT_BASES`, `DEFAULT_SUFFIXES`, `DEFAULT_CONTEXTS`, the concurrency level list, the mixed-traffic default, the decode corpus's weighted case list, and the tool-eval scenario assertion) under the protocol `repeats=3`, `prefill_warmups=1`, `tool_eval_runs=3`. Warmups, primes and lifecycle probes are recorded but excluded from the total.

- Weighted decode cases from the corpus: code, code-reasoning, math, fable, hello, topic, structured-json, structured-json-schema, multilingual
- Derived total, both arms: **359** performance records (30 + 90 + 135 + 27 + 45 + 2 + 30)
- Tool-eval: **88 scenarios x 3 runs = 264** scenario-runs, counted separately from the performance ledger.

| Arm | Family | Expected | Actual | Status | Derivation |
|---|---|---:|---:|---|---|
| v10-exl3-compact-tp3 | Headline decode (dSpark) | 30 | 0 | PENDING | (9 weighted + 1 counting) x 3 repeats |
| v10-exl3-compact-tp3 | Prefill matrix | 90 | 0 | PENDING | 5 bases x 6 suffixes = 30 cells x 3 timed |
| v10-exl3-compact-tp3 | Retained decode (prime contexts) | 135 | 0 | PENDING | 5 prime contexts [0, 32768, 65536, 131072, 262144] x 9 cases x 3 repeats |
| v10-exl3-compact-tp3 | Retained decode 2K control | 27 | 0 | PENDING | 1 control context(s) [2048] x 9 cases x 3 repeats |
| v10-exl3-compact-tp3 | Concurrency counting/code/topic | 45 | 0 | PENDING | 3 cases (counting/code/topic) x 5 levels [1, 2, 4, 8, 16] x 3 repeats |
| v10-exl3-compact-tp3 | Mixed traffic | 2 | 0 | PENDING | adaptive-mixed batches at concurrency [4, 16] |
| v10-exl3-compact-tp3 | Target-only decode (dSpark off) | 30 | 0 | PENDING | (9 weighted + 1 counting) x 3 repeats, DSPARK=off |
| v10-exl3-compact-tp3 | Startup / memory | — | 0 | NOT RECORDED | diagnostic; not part of the performance record ledger |
| v10-exl3-compact-tp3 | Kernel / tile diagnostics | — | 0 | NOT RECORDED | component microbenchmark; not serving throughput |

- **v10-exl3-compact-tp3**: no performance records present. PENDING until the raw manifests arrive; the expectation (359) is stated so the shortfall is visible, not hidden.

## Headline decode (dSpark, C1)

Median observed decode tokens/s from the timed samples. `N/A` means the family was not measured; no value is carried over from another arm.

| Case | v10-exl3-compact-tp3 |
|---|---:|
| code | — |
| code-reasoning | — |
| math | — |
| fable | — |
| hello | — |
| topic | — |
| structured-json | — |
| structured-json-schema | — |
| multilingual | — |
| counting | — |
| **Weighted (excl. counting)** | — |

| Arm | Timed samples | Serving completed | Objective passed | Objective assessed | Harness passed |
|---|---:|---:|---:|---:|---|
| v10-exl3-compact-tp3 | PENDING | PENDING | PENDING | PENDING | PENDING |

`Objective passed` is out of `Objective assessed`. Samples without an objective check are **unassessed, not passes**; a completed sample is not a passed sample.

## Within-arm repeat spread

Weighted-decode repeat values in recorded order, so the spread behind each median is visible.

| Arm | Repeats | Spread |
|---|---:|---:|
| v10-exl3-compact-tp3 | PENDING | PENDING |

## Prefill matrices

Median effective prefill tokens/s per retained-base/suffix cell. An incomplete matrix is provisional and is labelled so.

### v10-exl3-compact-tp3

PENDING — no prefill package present.

## Decode over retained context

Weighted nine-category dSpark tokens/s with verified prefix reuse, including the separate 2K control, which is a page-size-shaped prime and not a general long-context claim.

| Retained base | v10-exl3-compact-tp3 |
|---|---:|
| PENDING | PENDING |

## Concurrency scaling

Median aggregate tokens/s for counting / code / topic. Counting, code and topic are reported separately because they are different request shapes.

| Case | Concurrency | v10-exl3-compact-tp3 |
|---|---:|---:|
| PENDING | — | PENDING |

## Mixed traffic

Adaptive mixed batches; these are whole-batch aggregate rates, not per-stream C1 rates.

| Concurrency | v10-exl3-compact-tp3 |
|---:|---:|
| PENDING | PENDING |

## Target-only decode (dSpark off)

The same deployment relaunched with dSpark disabled; this is the control against the headline decode, and shares the decode nonce seed so the two are matched.

| Arm | Weighted tok/s | Timed samples | Case medians |
|---|---:|---:|---|
| v10-exl3-compact-tp3 | PENDING | PENDING | PENDING |

## Startup and memory diagnostics

Reservation, device-budget, occupancy and KV lines captured at launch. These are diagnostics and are excluded from the performance-record ledger; the weight-only admission figure is admission arithmetic, not a runtime fit.

| Arm | Weight-only remote bytes | Device budget bytes | Weight margin bytes | GPU memory rows | Spark budget lines |
|---|---:|---:|---:|---:|---|
| v10-exl3-compact-tp3 | PENDING | PENDING | PENDING | PENDING | PENDING |

- **v10-exl3-compact-tp3** resolved ceiling: reservation `32GiB`, KV pool `2GiB`, prefill batch `256`.

## Kernel and tile diagnostics

FFN/expert component microbenchmark. This is **not** serving throughput and is excluded from the performance ledger.

PENDING — no kernel/tile diagnostic package present.

## Tool-call evaluation

Per-run scores plus the non-pass scenario detail behind every run. Tool-eval is counted separately from the performance ledger: 88 scenarios x 3 runs = 264 scenario-runs per arm. Run scores move with sampling, so every run is listed rather than averaged.

### v10-exl3-compact-tp3

PENDING — no tool-eval package present.

## Quality, checks and retained failures

Every raw sample that failed or did not qualify is listed. A record without an objective check is unassessed, not a pass.

- No failed or non-passing raw performance records are present. Absence here is not a pass.

- No manifest-declared negatives are recorded. Absence here is not a pass.

## Missing-data index

Every declared family/artifact that rendered as a pending or em-dash cell, so nothing is omitted silently.

| Arm | Family | Declared | State |
|---|---|---|---|
| — | — | — | every declared artifact was present |

A family that is absent from both the manifest and the raw evidence is shown as **PENDING** in the accounting table above; it is a deferred measurement, not a zero and not a pass.

## Raw record digests

| Arm | Artifact | SHA-256 |
|---|---|---|

## Derived expectation and its sources

This table is the arithmetic behind the expected counts. It is recomputed at render time from the scripts below, so the expectation moves with the harness instead of drifting from it.

| Family | Expected (per arm) | Derivation | Source |
|---|---:|---|---|
| Headline decode (dSpark) | 30 | (9 weighted + 1 counting) x 3 repeats | `scripts/bench-ds41-release-decode.py` |
| Prefill matrix | 90 | 5 bases x 6 suffixes = 30 cells x 3 timed | `scripts/bench-ds41-release-prefill-matrix.py` |
| Retained decode (prime contexts) | 135 | 5 prime contexts [0, 32768, 65536, 131072, 262144] x 9 cases x 3 repeats | `scripts/bench-ds41-release-retained-decode.py` |
| Retained decode 2K control | 27 | 1 control context(s) [2048] x 9 cases x 3 repeats | `scripts/bench-ds41-release-retained-decode.py` |
| Concurrency counting/code/topic | 45 | 3 cases (counting/code/topic) x 5 levels [1, 2, 4, 8, 16] x 3 repeats | `scripts/bench-ds41-concurrent-api.py` |
| Mixed traffic | 2 | adaptive-mixed batches at concurrency [4, 16] | `scripts/bench-ds41-adaptive-mixed.py` |
| Target-only decode (dSpark off) | 30 | (9 weighted + 1 counting) x 3 repeats, DSPARK=off | `scripts/bench-ds41-release-decode.py` |
| Startup / memory | — | diagnostic; not part of the performance record ledger | `coordinator/worker reservation and device-budget logs` |
| Kernel / tile diagnostics | — | component microbenchmark; not serving throughput | `python/tools/bench_tp_ep_kernel.py` |
| **Total (performance)** | **359** | 30 + 90 + 135 + 27 + 45 + 2 + 30 | |

| Source script / corpus | SHA-256 |
|---|---|
| `scripts/bench-ds41-adaptive-mixed.py` | `228d134e8fbb5f5ec39ffe25b64b35d96634613cc8e712be3feadf866a50d511` |
| `scripts/bench-ds41-concurrent-api.py` | `c84fee3d1994410f2d5f6f1dd05fcc1ab31fa6942a2a413b9905be5a94b839a4` |
| `scripts/bench-ds41-release-decode.py` | `3c0a9b937779565bdcdd9c0de89e50d247651db3eae999974b5071c0f05015de` |
| `scripts/bench-ds41-release-prefill-matrix.py` | `24d697db6211204fdbde24e57fa8d2f538a3f690fc6559da7748f0156d2cf5a6` |
| `scripts/bench-ds41-release-retained-decode.py` | `180d0a6b4dcb053c8e4ad21773336788c21cd205ed8cafc9ff3e558ea3cf168e` |
| `scripts/fixtures/release-semantic-corpus.json` | `1972e572fbf9d468cd86c2db5d3d3de60641329edca152fe013a179eb55001de` |
| `scripts/qualify-ds41-tool-eval.py` | `2781bb949bc7d0abe5b32b4a9dc2102ba81809cd9355c7ecc87aff3c76df5333` |

## Reconciliation with the campaign expected-counts document

Source: `/home/tj/Developer/ds41rt/runs/v10-exl3-tp3/expected-counts.json`. The campaign's own mechanically-derived expected-counts document is compared shape by shape against this renderer's derivation, so a divergent breakdown is surfaced rather than hidden behind a total that happens to agree.

| Shape | Basis | Expected-counts document | This renderer | State |
|---|---|---:|---:|---|
| decode | — | 30 | 30 | match |
| prefill_timed | — | 90 | 90 | match |
| retained | prime | 135 | 135 | match |
| retained_control_2k | — | 27 | 27 | match |
| target_only | — | 30 | 30 | match |
| concurrency | — | 45 | 45 | match |
| mixed | — | 2 | 2 | match |
| performance total | — | 359 | 359 | match |
| tool_eval scenario-runs | — | 264 | 264 | match |

Every declared shape reconciles exactly. The retained family is stated in whichever basis the source document uses - a six-context lump (162) or the five prime contexts (135) plus the separate 2K control (27) - and the `basis` column records which one was compared, so the split form used by the per-context tables is never mistaken for a disagreement.

